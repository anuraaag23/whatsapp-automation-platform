import { ComingSoon } from '../_components/ComingSoon';

export default function SuperAdminWhatsappPage() {
  return (
    <ComingSoon
      title="WhatsApp Monitoring"
      note="Would show connected/disconnected accounts, webhook activity, and failed-message diagnostics across every organization, reusing WhatsappAccount and Message data already collected per-org."
    />
  );
}
