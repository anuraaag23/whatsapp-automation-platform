import { ComingSoon } from '../_components/ComingSoon';

export default function SuperAdminAutomationsPage() {
  return (
    <ComingSoon
      title="Automation Monitoring"
      note="The Overview page already surfaces platform-wide run/failure counts (real data, from AutomationRun). This page would add per-automation drill-down and failure diagnostics on top of that."
    />
  );
}
