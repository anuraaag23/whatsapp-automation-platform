import { ComingSoon } from '../_components/ComingSoon';

export default function SuperAdminAnalyticsPage() {
  return (
    <ComingSoon
      title="Platform Analytics"
      note="Would chart organization/user growth and message volume over time, reusing the existing per-org analytics module's query patterns rather than duplicating them."
    />
  );
}
