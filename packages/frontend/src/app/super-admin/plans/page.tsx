import { ComingSoon } from '../_components/ComingSoon';

export default function SuperAdminPlansPage() {
  return (
    <ComingSoon
      title="Plans"
      note="Depends on the same Plan data model as Limits & Quotas — the two should ship together rather than this page inventing its own separate shape."
    />
  );
}
